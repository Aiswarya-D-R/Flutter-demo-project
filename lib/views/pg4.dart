import 'package:flutter/material.dart';

class Pg4 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Edit pg")),
    );
  }
}
