import 'package:flutter/material.dart';

class Pg1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Home pg")),
    );
  }
}
